apiVersion: kube-shard.konflux-ci.dev/v1alpha1
kind: APIShard
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: apishard-sample
spec:
  # TODO(user): Add fields here
